# 普通文档上线前转换 SOP

## 1. 目的

本 SOP 用于将普通文档整理成符合当前教程站点展示规范的 Markdown 文件，
并通过本机 PicGo 将本地图片、视频和附件上传到阿里云 OSS。

执行完成后，应交付：

1. 一份可被 VitePress 正常构建和展示的 Markdown 文件。
2. 文档引用的全部线上资源地址。
3. 一份素材上传映射和检查结果，便于复核。

## 2. 当前环境与固定约定

- 教程源码目录：`F:\教程web\learning-docs-main`
- 教程正文目录：`F:\教程web\learning-docs-main\docs`
- 站点框架：VitePress
- 站点主题：`doc-theme-323`
- PicGo 程序：`C:\Users\admin\AppData\Local\Programs\PicGo\PicGo.exe`
- PicGo 本地服务：`http://127.0.0.1:36677`
- 当前图床：阿里云 OSS
- 资源访问域名：`https://arkimg.ark.online`
- 图片转换插件：`picgo-plugin-compress` 1.4.0
- 图片转换模式：`image2webp`
- 当前 WebP 质量参数：75

不得在文档、日志或交付说明中输出 OSS 的 AccessKey、Secret、Bucket
凭据等敏感配置。上传应复用 PicGo 已保存的配置。

## 3. 输入材料

开始转换前，确认以下输入：

- 原始文档或正文内容。
- 图片、视频和附件所在的本地目录。
- 最终 Markdown 文件的目标目录和文件名。
- 原文中素材与段落、步骤之间的对应关系。
- 是否需要加入站点侧边栏。没有明确要求时，不修改
  `docs/.vitepress/config.mts`。

若素材和正文的对应关系无法可靠判断，应先完成能够确认的转换，并将有歧义的
素材列入待确认清单，不得随意插入。

## 4. 总体流程

```text
检查输入材料
    ↓
整理正文结构和素材清单
    ↓
识别已上线资源与待上传资源
    ↓
通过 PicGo 上传本地素材
    ↓
记录“本地文件 → 线上 URL”映射
    ↓
转换 Markdown 格式和媒体排版
    ↓
检查链接、结构和本地路径残留
    ↓
执行 VitePress 构建
    ↓
交付 Markdown、上传映射和检查结果
```

## 5. 第一步：检查原文和素材

### 5.1 不直接修改原始材料

默认保留用户提供的原始文档和素材，只新建或编辑目标 Markdown 文件。
不要覆盖、重命名或删除原始文件，除非用户明确要求。

### 5.2 建立素材清单

逐项记录下列信息：

| 序号 | 本地文件 | 类型 | 对应章节或步骤 | 展示方式 | 上传状态 | 线上 URL |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | `示例截图.png` | 图片 | 步骤 1 | 单图 | 待上传 |  |
| 2 | `调整前.jpg` | 图片 | 效果对比 | 双图表格左列 | 待上传 |  |
| 3 | `演示.mp4` | 视频 | 完整教程视频 | 视频播放器 | 待上传 |  |
| 4 | `模板.zip` | 附件 | 素材下载 | 下载链接 | 待上传 |  |

检查素材数量是否和原文中的引用数量一致。重点排查：

- 文中提到“如下图”但没有对应图片。
- 存在素材文件但正文没有引用位置。
- 同一个素材被重复引用或重复上传。
- 文件名相同但内容不同。
- 图片、视频或附件已经使用 `https://arkimg.ark.online` 地址。

已经使用有效线上地址的资源默认保留，不重复上传。

### 5.3 对素材分类

按照扩展名分类，不只依靠文件图标：

- 常规图片：`.png`、`.jpg`、`.jpeg`、`.webp`
- 其他图片：`.gif`、`.svg` 等
- 视频：`.mp4`、`.webm`、`.mov` 等
- 附件：`.zip`、`.rar`、`.7z`、`.fbx`、`.psd`、`.pdf` 等

当前 compress 插件只会对 `.png`、`.jpg`、`.jpeg`、`.webp`
执行 WebP 转换；其他格式不会转换为 WebP。

## 6. 第二步：整理正文结构

### 6.1 内容原则

- 保留原文事实、参数、操作顺序和注意事项。
- 可以修正明显的错别字、重复标点、空格和层级问题。
- 不得臆造操作步骤、参数、资源地址或产品行为。
- 不确定的专有名词、版本号和数值保持原样，并列入待确认项。
- 界面名称的括号形式尽量统一，例如“单击【资源上传】”。

### 6.2 标题结构

一篇文档只使用一个一级标题：

```markdown
# 文档标题

## 1. 一级章节

### 1.1 具体操作
```

不要从 `#` 直接跳到 `###`。标题前后留空行，不使用单纯加粗代替标题。

### 6.3 段落和列表

- 一个段落表达一个完整主题。
- 连续操作使用有序列表。
- 并列规则或条件使用无序列表。
- 列表项需要包含多段文字、图片或代码时，保持 Markdown 缩进一致。
- Markdown 表格前后各留一个空行，避免表格被识别成普通文本。

### 6.4 加粗、行内代码和链接

界面重点、限制条件和关键结论可使用加粗：

```markdown
上传后资源**不能直接覆盖更新**。
```

文件名、属性名、类名、函数名和短代码使用行内代码：

```markdown
将脚本命名为 `PlayerControl`，然后实现 `onStart` 方法。
```

普通链接使用有意义的链接文字：

```markdown
[查看服装材质教程](https://learning.ark.online/example.html)
```

避免使用“点这里”作为唯一说明；读者应能从链接文字判断目标内容。

### 6.5 提示和注意事项

根据内容严重程度选择提示块：

```markdown
::: tip 提示

这里放操作建议、阅读时间或补充技巧。

:::

::: info 补充说明

这里放背景信息或扩展资料。

:::

::: warning 注意

这里放容易出错的操作、限制条件或兼容性问题。

:::

::: danger 重要警告

这里放可能导致数据丢失、资源失效或严重错误的内容。

:::
```

较长的完整代码或补充材料可以折叠：

````markdown
::: details 完整代码（点击展开）

```typescript
// 完整代码
```

:::
````

提示块的开始和结束标记应单独成行，内容与标记之间保留空行。

### 6.6 代码块

代码块必须标明语言，例如 `typescript`、`json`、`bash` 或 `text`：

````markdown
```typescript
@Component
export default class Example extends Script {
  protected onStart(): void {}
}
```
````

不要把长代码用截图代替；若原文只有代码截图且无法还原，保留图片并说明内容。

## 7. 第三步：通过 PicGo 上传素材

### 7.1 上传前检查 PicGo 服务

确认 PicGo 正在运行，并检查本地端口：

```powershell
Test-NetConnection -ComputerName 127.0.0.1 -Port 36677 `
  -InformationLevel Quiet
```

也可以调用心跳接口：

```powershell
Invoke-RestMethod `
  -Method Post `
  -Uri 'http://127.0.0.1:36677/heartbeat'
```

若 PicGo 未运行，可以启动：

```powershell
Start-Process `
  -FilePath 'C:\Users\admin\AppData\Local\Programs\PicGo\PicGo.exe' `
  -WindowStyle Hidden
```

等待服务可用后再上传。不要为了排查问题读取或打印完整 PicGo 配置文件。

### 7.2 图片上传

对常规图片使用 PicGo 本地上传接口。建议先上传一张图片作为探测，再进行批量上传：

```powershell
$requestBody = @{
  list = @('F:\素材\操作截图.png')
} | ConvertTo-Json

$uploadResult = Invoke-RestMethod `
  -Method Post `
  -Uri 'http://127.0.0.1:36677/upload' `
  -ContentType 'application/json' `
  -Body $requestBody `
  -TimeoutSec 600
```

期望响应结构：

```json
{
  "success": true,
  "result": ["https://arkimg.ark.online/example.webp"]
}
```

只有同时满足以下条件，才视为上传成功：

- `success` 为 `true`。
- `result` 中 URL 数量与提交文件数量一致。
- 每个 URL 都能对应到正确的本地文件。
- 常规图片返回地址的扩展名为 `.webp`。

批量上传时，按请求中的文件顺序记录返回 URL，不要只凭相似文件名匹配。
某一批次失败后应缩小为单文件重试，以定位具体素材。

### 7.3 视频、附件和其他格式图片上传

视频和附件必须保留原扩展名，不执行 WebP 转换。

当前 compress 插件虽然会提示“不支持的格式，跳过压缩”，但其后续逻辑仍会尝试
读取图片尺寸，因此不能默认认为 `.mp4`、`.zip` 等文件能在启用 compress
转换器时正常通过同一接口。执行时采用以下顺序：

1. 先使用一个可丢弃的测试素材验证当前 PicGo 对该扩展名的实际上传能力。
2. 若能够上传，确认返回 URL 保留原扩展名后再上传正式素材。
3. 若上传失败，使用临时 PicGo 配置将转换器设为内置 `path`，上传视频或附件，
   完成后恢复 `compress`；不要直接覆盖用户当前配置。
4. 若临时配置方式不可用，再改用 PicGo 界面分批上传，并在上传前暂停 compress，
   上传后立即恢复。
5. 不得为了绕过问题把视频或附件改名为图片扩展名。

切换转换器、修改 PicGo 配置或改用 OSS 工具前，应先说明原因，并保证变更可恢复。
不要在没有验证的情况下宣称非图片素材已经上传成功。

### 7.4 上传后的映射记录

为每个上传成功的素材记录一一对应关系：

```text
F:\素材\操作截图.png
→ https://arkimg.ark.online/操作截图.webp

F:\素材\演示.mp4
→ https://arkimg.ark.online/演示.mp4
```

出现同名覆盖风险时停止该文件的上传，先确认 OSS 命名规则或为文件生成不会冲突的
名称。不要覆盖无法确认归属的线上资源。

## 8. 第四步：将资源写入 Markdown

### 8.1 单张图片

普通单图优先使用 Markdown 图片语法，替代文字应描述图片内容：

```markdown
![资源导入窗口](https://arkimg.ark.online/resource-import.webp)
```

图片单独成段，并放在它所说明的操作步骤之后。

需要控制显示大小时使用 HTML：

```html
<img
  src="https://arkimg.ark.online/resource-import.webp"
  alt="资源导入窗口"
  style="zoom:67%;"
/>
```

只有确实需要缩放时才使用 `zoom`。比例应根据图片原始尺寸和正文可读性调整，
不能机械地给所有图片设置同一比例。

### 8.2 多张图片同一行展示

两张或多张图片需要并排时，使用 Markdown 表格固定布局，不直接连续书写多个
`<img>` 标签。

带对比说明的双图：

```markdown
| 调整前 | 调整后 |
| :---: | :---: |
| ![调整前](图片地址1) | ![调整后](图片地址2) |
```

纯图片网格：

```markdown
| ![示例1](图片地址1) | ![示例2](图片地址2) |
| :---: | :---: |
| ![示例3](图片地址3) | ![示例4](图片地址4) |
```

表格中的图片需要分别控制尺寸时：

```markdown
| 人形 | 四足 |
| :---: | :---: |
| <img src="图片地址1" alt="人形" style="zoom:20%;" /> | <img src="图片地址2" alt="四足" style="zoom:20%;" /> |
```

排版规则：

- 操作截图和对比图通常使用两列或三列。
- 小尺寸图标可以使用四至六列。
- 同一行的图片应具有相同语义层级，例如“调整前/调整后”。
- 表格每一行的列数必须一致。
- 表格中需要换行时使用 `<br />`，不能直接插入普通换行。
- 图片内文字较多时减少列数，保证线上能够看清。
- 不使用空白图片凑齐列数；最后一行缺图时保留空单元格。

### 8.3 视频

视频使用带控制栏的完整 HTML 标签：

```html
<video controls src="https://arkimg.ark.online/tutorial.mp4"></video>
```

视频单独成段，前面用正文或小标题说明内容。统一使用开始、结束标签，避免使用
自闭合形式。不得把视频写成 Markdown 图片。

### 8.4 下载附件

附件使用普通链接，并在链接文字中说明用途或格式：

```markdown
[下载妆容制作模板（ZIP）](https://arkimg.ark.online/makeup-template.zip)
```

表格内的附件也使用链接：

```markdown
| 文件说明 | 下载地址 |
| --- | --- |
| 女性基础模型 | [下载模型文件（RAR）](附件地址) |
```

## 9. 第五步：质量检查

### 9.1 内容检查

逐项确认：

- 标题层级连续，全文只有一个一级标题。
- 操作步骤顺序与原文一致。
- 参数、版本号、路径和专有名词没有被误改。
- 加粗标记成对，行内代码反引号成对。
- 提示块类型合理，开始和结束标记完整。
- 代码块围栏成对并标明语言。
- 图片、视频和附件均放在对应内容附近。
- 多图表格列数一致，图片说明和顺序正确。

### 9.2 资源检查

- 素材清单中的每个本地文件都有明确处理结果。
- Markdown 中不再残留 `C:\...`、`D:\...`、`F:\...` 等本地素材路径。
- 没有 `file://`、临时目录或编辑器缓存路径。
- 新上传资源使用 `https://arkimg.ark.online` 地址。
- 常规图片为 `.webp`；视频和附件保留原格式。
- 所有线上 URL 可以访问，且返回内容与引用类型一致。
- 同一个资源没有因重复处理产生多个无意义的 URL。

可使用以下命令检查目标文档是否残留 Windows 本地路径：

```powershell
rg -n '[A-Za-z]:\\|file://' 'F:\目标文档.md'
```

该命令没有输出时，表示未发现相应路径模式。

### 9.3 构建检查

在仓库根目录执行：

```powershell
npm run docs:build
```

构建必须正常结束。若构建失败：

1. 优先检查 Markdown 表格、代码块、提示块和 HTML 标签是否闭合。
2. 检查标题或链接中是否存在破坏语法的特殊字符。
3. 只修复与本次目标文档有关的问题；不要顺手修改无关文件。
4. 修复后重新运行构建。

仓库已启用 `ignoreDeadLinks`，因此构建成功不代表所有外部资源 URL 都有效；
外部资源仍需单独检查。

### 9.4 页面预览

当多图表格、较大图片或视频较多时，启动本地预览：

```powershell
npm run docs:dev
```

重点观察：

- 图片是否过大、过小或被压缩到无法阅读。
- 多图表格在正文宽度内是否排列正常。
- 表头和图片的对应关系是否清晰。
- 视频是否显示控制栏并能加载。
- 提示块、列表和代码块是否被主题正确渲染。

完成检查后停止开发服务，避免遗留后台进程。

## 10. 第六步：交付

交付说明应简洁列出：

- 最终 Markdown 文件的绝对路径。
- 新上传的图片、视频和附件数量。
- PicGo 返回 URL 的映射记录位置或摘要。
- `npm run docs:build` 的结果。
- 是否完成页面预览。
- 尚未上传、无法匹配或需要用户确认的项目。

不要把“文件已经生成”和“文档已经上线”混为一谈。只有完成站点发布流程后才能称为
“已上线”；本 SOP 默认完成的是上线前转换和验证。

## 11. 异常处理

### 11.1 PicGo 接口不可用

1. 检查 PicGo 进程和 `36677` 端口。
2. 启动 PicGo 后重新调用 `/heartbeat`。
3. 查看 PicGo 界面或日志中的明确错误，但不要输出敏感配置。
4. 服务恢复前继续整理正文和素材清单，不重复盲目提交上传请求。

### 11.2 图片上传后不是 WebP

1. 检查源文件扩展名是否属于插件支持范围。
2. 检查 PicGo 当前转换器是否为 `compress`。
3. 检查 compress 模式是否为 `image2webp`。
4. 单文件重试并检查返回 URL。
5. 原因未确认前，不批量替换 Markdown 链接。

### 11.3 视频或附件上传失败

按照第 7.3 节处理。保留原文件，不改扩展名，不把失败请求产生的空地址写入文档。

### 11.4 素材无法对应正文

在目标位置写清晰的临时注释，例如：

```markdown
<!-- TODO：确认此处应使用哪一张操作截图 -->
```

同时在交付说明中列出待确认项。确认后应删除临时注释。

### 11.5 上传成功但 Markdown 写入失败

不要重复上传。应从本次映射记录中恢复 URL，继续编辑目标 Markdown 文件。

## 12. Codex 执行要求

后续 Codex 接到文档转换任务时，应遵守以下要求：

1. 先阅读本 SOP，再检查用户提供的原文和素材目录。
2. 先建立素材清单，再执行上传和 Markdown 替换。
3. 用户明确要求上传和转换后，可以使用现有 PicGo 配置执行上传，无需再次询问。
4. 不读取或输出 OSS 密钥，不在仓库中创建包含凭据的配置文件。
5. 已有线上资源不重复上传，本地原始素材不覆盖、不删除。
6. 图片上传使用 PicGo 的 compress 转换器并确认返回 WebP URL。
7. 视频和附件先验证兼容性，确保保留原格式后再批量处理。
8. 多图并排使用表格，普通单图单独成段，视频使用 `<video controls>`。
9. 编辑完成后检查本地路径残留并执行 VitePress 构建。
10. 构建和检查未通过时不得声称转换已经完成。
11. 不修改与目标文档无关的仓库文件。
12. 最终报告文件位置、上传数量、验证结果和待确认项。

## 13. 推荐的任务指令模板

后续可以用以下方式向 Codex 下达任务：

```text
请阅读仓库根目录的 DOCUMENT_CONVERSION_SOP.md，并按照其中流程执行。

原始文档：<原始文档绝对路径>
素材目录：<素材目录绝对路径>
目标文件：<目标 Markdown 绝对路径>

请整理正文格式，通过现有 PicGo 上传需要的图片、视频和附件，将资源地址写入
Markdown，完成多图表格排版，并执行上线前检查和 VitePress 构建。
```
